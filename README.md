🏦 Mortgage Calculator API

Небольшой сервис для расчёта ипотеки — создаёт расчёт, сохраняет данные в базе и позволяет скачать результат в виде RTF-файла.

🚀 Используемые технологии

NestJS

MySQL + Drizzle ORM

TypeScript

Express (для скачивания файла)

📡 Эндпойнты API
POST /mortgage

Создаёт новый расчёт ипотеки.

Пример запроса:

{
  "userId": 1,
  "cost": 6000000,
  "initialPayment": 600000,
  "years": 20,
  "rate": 11.06
}


Возвращает:

ID профиля

ID расчёта

Месячный платёж

Полный график платежей

GET /mortgage/:id/rtf

Генерирует и отдаёт RTF-файл с результатами расчёта:

Общая информация

Ежемесячный платёж

Первые 12 месяцев графика платежей

Пример:

GET /mortgage/1/rtf


Браузер или Postman предложит скачать файл вида:

mortgage_1.rtf

▶️ Запуск проекта
pnpm install
pnpm db:generate
pnpm db:migrate
pnpm dev
